import { AccordionSection } from './AccordionSection';

export function LoadList() {
  return (
    <AccordionSection value="loads" title="Loads" count={0}>
      <p className="px-2 py-1 text-[10px] text-gray-600">
        Load editing coming in Phase 2
      </p>
    </AccordionSection>
  );
}
